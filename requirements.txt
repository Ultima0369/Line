# Line Framework — 核心依赖
# 安装: pip install -r requirements.txt

httpx>=0.27.0            # 异步 HTTP 客户端（调用 DeepSeek API，不阻塞 event loop）
pyyaml>=6.0             # 配置管理
pydantic>=2.0.0         # 数据模型

# === 可选依赖（按需安装） ===
# psutil>=5.9.0          # 系统传感器（CPU/内存）
# pyserial>=3.5          # 串口传感器（Arduino 等）
# smbus2>=0.4.0          # I2C 传感器（树莓派）
# RPi.GPIO>=0.7.0        # GPIO（树莓派）
# transformers>=4.36.0   # 本地模型（需 torch）
# torch>=2.0.0           # 深度学习框架
# rich>=13.0.0           # 终端美化（可选）
